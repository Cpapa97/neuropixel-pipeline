# Generate or validate a pipeline config file from PipelineConfig json serialization
# this can either be done entirely custom with a name (like atlab) and a json dictionary passed in
# or possibly have some way of selecting from the modules, although maybe only atlab will do it that way?

from . import PipelineConfig

